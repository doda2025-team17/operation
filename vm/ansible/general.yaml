# General setup for all nodes
- name: General node setup
  hosts: all
  become: yes
  vars:
    keys: "{{ lookup('fileglob', 'ssh-keys/*.pub', wantlist=True) }}"
  tasks:
    # Step 3: Basic system setup
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install basic packages
      apt:
        name:
          - curl
          - vim
          - net-tools
        state: present

    # Step 4: Register SSH keys
    - name: Add team SSH keys
      authorized_key:
        user: vagrant
        state: present
        key: "{{ lookup('file', item) }}"
      loop: "{{ keys }}"

    # Step 5: Disable SWAP
    - name: Disable swap at runtime
      shell: swapoff -a

    - name: Remove swap from /etc/fstab
      lineinfile:
        path: /etc/fstab
        regexp: '^\s*[^#].*swap'
        state: absent

    # Step 6: Load br_netfilter module
    - name: Create modules-load config for Kubernetes
      copy:
        dest: /etc/modules-load.d/k8s.conf
        content: |
          overlay
          br_netfilter

    - name: Load br_netfilter module
      modprobe:
        name: br_netfilter
        state: present

    - name: Load overlay module
      modprobe:
        name: overlay
        state: present

    # Step 7: Enable IPv4 forwarding and bridge settings
    - name: Enable IPv4 forwarding
      sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        state: present
        reload: yes

    - name: Enable bridge netfilter for iptables
      sysctl:
        name: net.bridge.bridge-nf-call-iptables
        value: '1'
        state: present
        reload: yes

    - name: Enable bridge netfilter for ip6tables
      sysctl:
        name: net.bridge.bridge-nf-call-ip6tables
        value: '1'
        state: present
        reload: yes

    # Step 8: Manage /etc/hosts with dynamic generation
    - name: Create dynamic hosts file
      template:
        src: hosts.j2
        dest: /etc/hosts
        owner: root
        group: root
        mode: '0644'
      vars:
        num_workers: "{{ num_workers | default(2) }}"